<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('app_home_page') }}">
            <img src="{{ asset('images/logo.png') }}" alt="FeelTheHoliday" height="40">
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_home_page') }}">Najblisze Eventy</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_home_page') }}">Archiwum</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_home_page') }}">Kontakt</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_home_page') }}">O nas</a>
                </li>
            </ul>
            <ul class="navbar-nav mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="btn btn-primary me-2" href="{{ path('app_home_page') }}">Kup Bilety</a>
                </li>
                <li class="nav-item">
                    <a class="btn btn-danger me-2" href="{{ path('app_home_page') }}">Panel Ogarniacza</a>
                </li>
                {% if app.user %}
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="userDropdown" role="button" data-bs-toggle="dropdown">
                        Zalogowany jako {{ app.user.firstName }}
                    </a>
                    <ul class="dropdown-menu dropdown-menu-end">
                        <li>
                            <a class="dropdown-item" href="{{ path('app_home_page') }}">Profil</a>
                        </li>
                        <li>
                            <a class="dropdown-item" href="{{ path('app_logout') }}">Wyloguj</a>
                        </li>
                    </ul>
                </li>
                {% else %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_login') }}">Zaloguj się</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_register') }}">Zarejestruj się</a>
                </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>